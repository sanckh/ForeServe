# Express server configuration
# Port the API server listens on
PORT=4000

# Allowed CORS origins (comma-separated). Use * to allow all origins in development.
CORS_ORIGIN=*

# Staff auth token for protected endpoints (claim/status/86). Change in production.
STAFF_TOKEN=changeme

# Mobile app can read this public env at build time to point to the API
# e.g., http://localhost:4000/api or your tunnel/remote URL
EXPO_PUBLIC_API_URL=http://localhost:4000/api
